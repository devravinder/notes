# Prompts

1. Format this markdown document with the following rules:

- Remove all tables and convert them into ordered lists
  - keep first column as list ietms & remainig columns as sub unordered lists
  - Keep shortcut + functionality on the same line as list item
- Replace all bold (`**text**`) with inline code style using backticks (`text`)
- Keep formatting clean, consistent, and readable
- Do not change the actual content
